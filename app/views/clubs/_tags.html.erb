<div class="col-md-12 view-tags">
  <h4><strong><i class="fa fa-tags" aria-hidden="true"></i>
    <%= t "tags" %></strong></h4>
    <p><% @club.tag_list.each do |tag| %>
      <span class="tag"><%= tag %></span>
    <% end %></p>
</div>
