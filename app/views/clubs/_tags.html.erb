<div class="col-md-12 view-tags">
  <h4 class="text-center left-title"><i class="fa fa-tags" aria-hidden="true"></i>
    <%= t "tags" %></h4>
    <p><% @club.tag_list.each do |tag| %>
      <span class="tag"><%= tag %></span>
    <% end %></p>
</div>
