<% if current_user.user_organizations.join? organization %>
  <%= link_to user_user_organizations_path(organization_id: organization.id),
    class: "btn btn-primary", method: :post, data: {confirm: t("answer_join")} do %>
    <i class="fa fa-sign-in"> <%= t "request_to_join" %></i>
  <% end %>
<% end %>
